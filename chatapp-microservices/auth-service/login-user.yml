info:
  name: login-user
  type: http
  seq: 2

http:
  method: POST
  url: http://localhost:4000/auth/login
  body:
    type: json
    data: |-
      {
          "email": "tahsin@example.com",
          "password": "password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
